import SwiftUI

enum Theme {
    /// PRD 主色 #4CAF50
    static let primary = Color("PrimaryGreen")
    /// 辅助浅绿 #81C784
    static let secondaryGreen = Color("SecondaryGreen")
    /// 背景绿白 #F1F8E9
    static let backgroundTint = Color("BackgroundTint")
    static let cardBackground = Color(.secondarySystemGroupedBackground)
    static let urgent = Color.red
    static let watch = Color.orange
    static let healthy = Color("PrimaryGreen")
    static let muted = Color.secondary
}
